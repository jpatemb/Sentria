# Copy this file to .env and fill in your key. .env is gitignored — never
# commit your real key.
#
# Only needed to run agent/orchestrator.py (the live orchestration loop);
# the offline test suite (pytest tests/) never reads this.
ANTHROPIC_API_KEY=sk-ant-your-key-here
